// yea turns out this wasn't necessary at all
// actually maybe it will helpful when it comes to hte ADTs


// Bookmark uri's are unique and identifying
var Bookmark = function(uri) {
    var uri = uri;
    
    this.getURI = function() {
        return uri;
    };

    var checkRep = function() {

    };
    checkRep();
};

// Circle names are unique and identifying
var Circle = function(name) {
    var name = name;

    this.getName = function() {
      return name;
    };
    var checkRep = function() {
    }
    checkRep();
}

